# pjm console app entry point
PDV_setup_kwargs={'entry_points': {'console_scripts':\
    ['pjm = aedev.project_manager.__main__:main',\
     'project-manager = aedev.project_manager.__main__:main', ]}}

# PDV_IGNORE_MISSING_IMPORTS=[]     # ignore imports missing related package in requirements.txt (+ ['setuptools'])

# required but not imported (mostly only using their CLI)
PDV_IGNORE_EXTRA_REQS=['coverage', 'flake8', 'mypy', 'pylint', 'pytest', 'pytest-cov', 'types-Pillow']
